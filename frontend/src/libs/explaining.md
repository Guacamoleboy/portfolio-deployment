### What is hooks/ ?

The hooks folder is for react specific behavior. Such as functions that uses useState, useEffect etc.

### What is libs/ ?

The libs folder is for functions that has no relationsship to anything other than doing the "hard labour". For example math calculations etc.